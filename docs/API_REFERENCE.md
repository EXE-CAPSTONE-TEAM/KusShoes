# KusShoes Platform API — API reference

> Generated by `python -m scripts.export_api_docs` from the OpenAPI schema; do not edit by hand. Live docs: `/docs` (Swagger UI) and `/openapi.json` (disabled in production).

Backend API của nền tảng KusShoes (thiết kế giày 3D tùy biến).

**Xác thực**
- Người dùng: `Authorization: Bearer <access_token>` (token sống ngắn, đổi bằng `/auth/refresh`;
  refresh token nằm trong cookie HttpOnly).
- Admin/Staff: đăng nhập tại `/admin/auth/login`, cùng dạng Bearer. Endpoint ghi dữ liệu yêu cầu
  role `admin` (staff nhận 403).
- Ứng dụng desktop / worker: header `X-Service-Token`.

**Định dạng lỗi**: mọi lỗi nghiệp vụ trả `{"code": "MA_LOI", "message": "..."}` kèm trường phụ
tùy lỗi (ví dụ `retry_after`, `terms`, `next_allowed_at`). Lỗi kiểm tra dữ liệu đầu vào (422) dùng
định dạng chuẩn của FastAPI (`detail`).

**Phân trang**: danh sách dùng con trỏ (`cursor`, `limit`) và trả `next_cursor` + `has_next`.

**Thời gian**: ISO 8601 có múi giờ; ngày nghiệp vụ tính theo GMT+7.

**Số tiền**: VND, số nguyên.

Field marks: `name?` = optional.

## Auth

Đăng ký, đăng nhập, OTP, 2FA, phiên đăng nhập, SSO cho editor, khôi phục mật khẩu/tài khoản.

### `POST /api/v1/auth/register` — Đăng ký tài khoản

Tạo tài khoản email/mật khẩu và gửi mã OTP xác minh. Bắt buộc `age_confirmed=true` (BR-02); ghi bản ghi đồng ý điều khoản, quyền riêng tư và độ tuổi (BR-89) và nguồn truy cập (UTM/mã giới thiệu). Giới hạn tần suất theo IP. Công khai.

Request body: `email`: email; `username`: string; `password`: string; `confirm_password`: string; `full_name`: string; `age_confirmed?`: boolean; `utm_source?`: string | null; `utm_campaign?`: string | null; `referral_code?`: string | null

Responses: `201` RegisterResponse

Response fields: `user_id`: uuid; `email`: string; `message`: string

### `POST /api/v1/auth/verify-otp` — Xác minh email bằng OTP

Xác minh mã OTP 6 số. Sai quá số lần cho phép sẽ khóa tạm (`OTP_LOCKED`); mã hết hạn trả `OTP_EXPIRED`. Công khai.

Request body: `user_id`: uuid; `otp_code`: string

Responses: `200` TokenResponse

Response fields: `access_token`: string; `token_type?`: string

### `POST /api/v1/auth/resend-otp` — Gửi lại OTP

Gửi lại mã xác minh cho tài khoản chưa xác thực, có giới hạn tần suất. Công khai.

Request body: `user_id`: uuid

Responses: `200` OTPResendResponse

Response fields: `message`: string; `resend_remaining`: integer

### `POST /api/v1/auth/login` — Đăng nhập

Đăng nhập email/mật khẩu. Trả access token và đặt refresh token vào cookie HttpOnly. Nếu bật 2FA trả `mfa_required` cùng challenge token để gọi `/auth/2fa/verify`. Khóa 15 phút sau 5 lần sai (BR-86); gửi email khi đăng nhập từ thiết bị mới (BR-14). Công khai.

Request body: `email`: email; `password`: string

Responses: `200` LoginResult

Response fields: `access_token?`: string | null; `token_type?`: string; `mfa_required?`: boolean; `challenge_token?`: string | null; `method?`: string | null

### `POST /api/v1/auth/2fa/verify` — Hoàn tất đăng nhập với 2FA

Nhận challenge token và mã TOTP/email hoặc mã khôi phục. Sai mã không làm mất challenge (giới hạn số lần thử). Công khai.

Request body: `challenge_token`: string; `code?`: string | null; `recovery_code?`: string | null

Responses: `200` TokenResponse

Response fields: `access_token`: string; `token_type?`: string

### `GET /api/v1/auth/google` — Bắt đầu đăng nhập Google

Chuyển hướng (302) tới màn hình đồng ý của Google (OAuth 2.0, có `state` chống CSRF). Công khai.

Responses: `200` object

### `GET /api/v1/auth/google/callback` — Callback đăng nhập Google

Google gọi về sau khi đồng ý (`code`, `state`); tạo hoặc nối tài khoản rồi trả access token (`is_new_user`, `linked`) và đặt refresh token vào cookie. Công khai.

Parameters: `code` (query, required), `state` (query, required)

Responses: `200` GoogleLoginResponse

Response fields: `access_token`: string; `token_type?`: string; `is_new_user`: boolean; `linked?`: boolean

### `POST /api/v1/auth/refresh` — Đổi refresh token lấy access token

Đọc refresh token từ cookie (hoặc body), xoay vòng token. Token đã dùng/không hợp lệ trả `AUTH_REFRESH_INVALID`. Công khai.

Request body: 

Responses: `200` AccessTokenResponse

Response fields: `access_token`: string; `token_type?`: string

### `POST /api/v1/auth/logout` — Đăng xuất

Thu hồi refresh token hiện tại và xóa cookie. Bearer.

Request body: 

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/auth/forgot-password` — Yêu cầu mã đặt lại mật khẩu

Gửi mã khôi phục 15 phút tới email nếu tài khoản hợp lệ; luôn trả cùng một thông báo để không lộ email nào tồn tại. Giới hạn theo IP và email. Công khai.

Request body: `email`: email

Responses: `202` ForgotPasswordResponse

Response fields: `message`: string

### `POST /api/v1/auth/reset-password` — Đặt lại mật khẩu

Đổi mật khẩu bằng mã khôi phục, thu hồi mọi phiên. Sai quá 5 lần trả `AUTH_RESET_LOCKED`. Công khai.

Request body: `email`: email; `otp_code`: string; `new_password`: string; `confirm_password`: string

Responses: `200` ForgotPasswordResponse

Response fields: `message`: string

### `GET /api/v1/auth/sessions` — Danh sách phiên đăng nhập

Các phiên (thiết bị) đang hoạt động của người dùng. Bearer.

Responses: `200` SessionListResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[SessionResponse]

### `DELETE /api/v1/auth/sessions` — Thu hồi mọi phiên

Đăng xuất khỏi tất cả thiết bị. Bearer.

Responses: `200` ForgotPasswordResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `DELETE /api/v1/auth/sessions/{session_id}` — Thu hồi một phiên

Đăng xuất một thiết bị cụ thể. Không tìm thấy trả `AUTH_SESSION_NOT_FOUND`. Bearer.

Parameters: `session_id` (path, required)

Responses: `200` ForgotPasswordResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/auth/editor/launch` — Tạo vé mở KusStudio

Tạo vé mở editor dùng một lần, sống ngắn, gắn với một dự án; trả URL/ticket để mở ứng dụng desktop. Bearer.

Request body: `project_id`: uuid

Responses: `200` EditorLaunchCreateResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `launch_ticket`: string; `desktop_url`: string; `expires_in`: integer

### `POST /api/v1/auth/editor/launch/claim` — Desktop nhận vé mở editor

Ứng dụng desktop trình vé để nhận thông tin phiên khởi tạo; vé chỉ dùng được một lần. Công khai.

Request body: `launch_ticket`: string; `code_challenge`: string; `code_challenge_method?`: string

Responses: `200` EditorLaunchClaimResponse

Response fields: `authorization_code`: string; `expires_in`: integer

### `POST /api/v1/auth/editor/launch/exchange` — Đổi vé lấy editor session

Đổi vé đã nhận lấy editor session token (scope theo quyền của người dùng). Công khai.

Request body: `authorization_code`: string; `code_verifier`: string

Responses: `200` EditorLaunchExchangeResponse

Response fields: `access_token`: string; `token_type?`: string; `expires_in`: integer; `user_id`: uuid; `project_id`: uuid; `scopes`: list[string]

### `GET /api/v1/auth/editor/session` — Phiên editor hiện tại

Trả thông tin và scope của editor session đang dùng. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Responses: `200` EditorSessionResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `user_id`: uuid; `project_id`: uuid; `scopes`: list[string]; `expires_at`: integer

### `POST /api/v1/auth/sso-token` — Tạo mã SSO mở editor

Tạo mã dùng một lần (sống ngắn) gắn với một dự án để mở KusStudio mà không nhập lại mật khẩu. Bearer.

Request body: `project_id`: uuid

Responses: `200` SSOCreateResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `sso_token`: string; `expires_in`: integer

### `POST /api/v1/auth/verify-sso` — Đổi mã SSO lấy phiên editor

Editor đổi mã SSO thành thông tin người dùng/dự án. Mã dùng một lần. Service token (`X-Service-Token`).

Parameters: `x-service-token` (header, required)

Request body: `sso_token`: string

Responses: `200` SSOVerifyResponse

Response fields: `user_id`: uuid; `project_id`: uuid; `email`: string; `username`: string

### `POST /api/v1/auth/desktop-session` — Đổi mã SSO lấy phiên desktop

Tương tự verify-sso nhưng trả access token cho ứng dụng desktop. Service token (`X-Service-Token`).

Request body: `sso_token`: string

Responses: `200` SSODesktopSessionResponse

Response fields: `access_token`: string; `token_type?`: string; `user_id`: uuid; `project_id`: uuid; `email`: string; `username`: string; `name`: string; `role`: string; `created_at`: date-time; `updated_at`: date-time

### `POST /api/v1/auth/restore-account/request` — Yêu cầu khôi phục tài khoản đã xóa

Gửi mã 6 số tới email nếu tài khoản đã xóa còn trong 30 ngày (BR-06). Luôn trả cùng một thông báo. Công khai.

Request body: `email`: email

Responses: `202` ForgotPasswordResponse

Response fields: `message`: string

### `POST /api/v1/auth/restore-account/confirm` — Xác nhận khôi phục tài khoản

Nhập email + mã để hủy xóa tài khoản; sau đó đăng nhập lại. Sai/hết hạn trả `AUTH_RESTORE_INVALID`. Công khai.

Request body: `email`: email; `otp_code`: string

Responses: `200` object

## Users

Hồ sơ cá nhân, quyền riêng tư, đồng ý dữ liệu, lịch sử đăng nhập, 2FA, xuất dữ liệu, xóa tài khoản.

### `GET /api/v1/users/me` — Hồ sơ của tôi

Thông tin tài khoản và gói hiện tại. Bearer.

Responses: `200` UserDetailResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `account_code`: string; `email`: string; `first_name`: string; `last_name`: string; `username`: string; `avatar_path`: string | null; `phone_number`: string | null; `bio`: string | null; `language`: string; `preferred_styles`: list[string]; `status`: string; `member_since`: date-time; `total_designs`: integer

### `PATCH /api/v1/users/me` — Cập nhật hồ sơ

Đổi tên, username (giới hạn 30 ngày/lần, BR-10, có từ khóa dành riêng). Bearer.

Request body: `first_name?`: string | null; `last_name?`: string | null; `username?`: string | null; `avatar_path?`: string | null; `phone_number?`: string | null; `bio?`: string | null; `language?`: string | null; `preferred_styles?`: list[string] | null

Responses: `200` UserDetailResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `account_code`: string; `email`: string; `first_name`: string; `last_name`: string; `username`: string; `avatar_path`: string | null; `phone_number`: string | null; `bio`: string | null; `language`: string; `preferred_styles`: list[string]; `status`: string; `member_since`: date-time; `total_designs`: integer

### `DELETE /api/v1/users/me` — Xóa tài khoản

Xóa mềm; cần xác thực lại (mật khẩu hoặc Google). Thu hồi mọi phiên; khôi phục được trong 30 ngày (BR-06). Bị chặn trong phiên đăng nhập thay. Bearer.

Request body: `password?`: string | null; `google_id_token?`: string | null

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/users/me/avatar` — Xin URL tải avatar

Trả presigned URL để tải ảnh đại diện lên. Bearer.

Request body: `filename`: string; `content_type`: string

Responses: `200` AvatarUploadResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `upload_url`: string; `file_path`: string; `expires_in?`: integer

### `DELETE /api/v1/users/me/avatar` — Xóa avatar

Xóa ảnh đại diện và tệp lưu trữ tương ứng. Bearer.

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `PUT /api/v1/users/me/password` — Đổi mật khẩu

Yêu cầu mật khẩu hiện tại; thu hồi mọi phiên khác. Bị chặn trong phiên đăng nhập thay. Bearer.

Request body: `current_password`: string; `new_password`: string; `confirm_password`: string

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `GET /api/v1/users/me/usage` — Mức sử dụng hạn mức

Số dự án, lượt xuất, AI credit trong chu kỳ hiện tại so với hạn mức gói. Bearer.

Responses: `200` UsageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `tier`: string; `max_projects`: integer | null; `max_exports_per_month`: integer | null; `projects_count`: integer; `exports_count`: integer; `ai_credits_used`: integer; `ai_credits_limit?`: integer | null

### `GET /api/v1/users/me/privacy` — Cài đặt quyền riêng tư

Mặc định tắt hết (BR-15). Bearer.

Responses: `200` PrivacySettingsResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `is_profile_public`: boolean; `show_designs_publicly`: boolean; `is_searchable`: boolean; `allow_analytics`: boolean; `allow_ads_personalization`: boolean

### `PATCH /api/v1/users/me/privacy` — Đổi cài đặt quyền riêng tư

Hồ sơ công khai, hiển thị thiết kế, cho phép tìm kiếm, phân tích, cá nhân hóa quảng cáo. Bearer.

Request body: `is_profile_public?`: boolean | null; `show_designs_publicly?`: boolean | null; `is_searchable?`: boolean | null; `allow_analytics?`: boolean | null; `allow_ads_personalization?`: boolean | null

Responses: `200` PrivacySettingsResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `is_profile_public`: boolean; `show_designs_publicly`: boolean; `is_searchable`: boolean; `allow_analytics`: boolean; `allow_ads_personalization`: boolean

### `GET /api/v1/users/me/consents` — Danh sách đồng ý

Đồng ý điều khoản, marketing, báo cáo học thuật, cookie (BR-89). Bearer.

Responses: `200` list[ConsentResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/users/me/consents` — Ghi/thu hồi đồng ý

Bật hoặc thu hồi một loại đồng ý tùy chọn. Bearer.

Request body: `type`: string; `doc_version?`: string; `granted?`: boolean

Responses: `200` ConsentResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `type`: string; `doc_version`: string; `channel`: string; `created_at`: date-time; `revoked_at`: date-time | null

### `GET /api/v1/users/me/login-history` — Lịch sử đăng nhập

Các lần đăng nhập thành công/thất bại, IP che octet cuối (BR-18); lưu 90 ngày. Bearer.

Responses: `200` LoginHistoryResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[LoginHistoryItem]

### `POST /api/v1/users/me/data-export` — Xuất dữ liệu cá nhân

Tạo file zip (hồ sơ, dự án, đồng ý, lịch sử đăng nhập) và trả URL tải 15 phút; tối đa 1 lần/24 giờ (BR-19). Bearer.

Responses: `200` DataExportResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `download_url`: string; `expires_in`: integer

### `GET /api/v1/users/me/2fa` — Trạng thái 2FA

Phương thức đang bật, email khôi phục. Bị chặn trong phiên đăng nhập thay. Bearer.

Responses: `200` TwoFactorStatusResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `enabled`: boolean; `method`: string | null; `recovery_email`: string | null; `recovery_email_verified`: boolean

### `POST /api/v1/users/me/2fa/recovery-email` — Đặt email khôi phục 2FA

Gửi mã xác nhận tới email khôi phục. Bearer.

Request body: `recovery_email`: email

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/users/me/2fa/recovery-email/verify` — Xác nhận email khôi phục

Nhập mã đã gửi. Bearer.

Request body: `code`: string

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/users/me/2fa/setup` — Bắt đầu thiết lập 2FA

Chọn `totp` (trả secret/URI để quét) hoặc `email` (gửi mã). Bearer.

Request body: `method`: string

Responses: `200` TwoFactorSetupResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `method`: string; `totp_secret?`: string | null; `provisioning_uri?`: string | null

### `POST /api/v1/users/me/2fa/enable` — Bật 2FA

Xác nhận mã đầu tiên; trả 10 mã khôi phục dùng một lần (chỉ hiện một lần). Bearer.

Request body: `method`: string; `code`: string

Responses: `200` TwoFactorEnableResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string; `recovery_codes`: list[string]

### `POST /api/v1/users/me/2fa/disable` — Tắt 2FA

Yêu cầu mật khẩu và mã 2FA. Bearer.

Request body: `password?`: string | null; `code?`: string | null

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/users/me/impersonation/end` — Kết thúc phiên đăng nhập thay

Chỉ dùng với token của phiên impersonation; ghi audit và gửi email thông báo cho khách. Bearer.

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `message`: string

## Projects

Dự án thiết kế, thùng rác, lưu thiết kế, bake/export 3D.

### `GET /api/v1/projects` — Danh sách dự án

Dự án chưa xóa của người dùng, phân trang con trỏ, mới sửa gần nhất trước. Bearer.

Parameters: `limit` (query), `cursor` (query)

Responses: `200` ProjectListResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[ProjectResponse]; `next_cursor`: string | null; `has_next`: boolean

### `POST /api/v1/projects` — Tạo dự án

Bị giới hạn theo `max_projects` của gói (`PROJ_QUOTA_EXCEEDED`). Bearer.

Request body: `name`: string; `description?`: string | null

Responses: `201` ProjectResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `status`: string; `is_locked`: boolean; `thumbnail_path`: string | null; `design_config?`: object | null; `editor_url`: string; `created_at`: date-time; `updated_at`: date-time

### `GET /api/v1/projects/trash` — Thùng rác

Dự án đã xóa, còn khôi phục được trong 30 ngày (BR-47). Bearer.

Parameters: `limit` (query), `cursor` (query)

Responses: `200` ProjectTrashListResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[TrashProjectResponse]; `next_cursor`: string | null; `has_next`: boolean

### `GET /api/v1/projects/{project_id}` — Chi tiết dự án

Kèm `design_config` và cờ `is_locked` (BR-27). Không phải chủ sở hữu trả 403. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` ProjectDetailResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `status`: string; `is_locked`: boolean; `thumbnail_path`: string | null; `design_config?`: object | null; `editor_url`: string; `created_at`: date-time; `updated_at`: date-time; `canonical_model_asset_id`: uuid | null; `revision`: integer

### `PATCH /api/v1/projects/{project_id}` — Cập nhật dự án

Đổi tên/mô tả. Dự án bị khóa trả `PROJECT_LOCKED`. Bearer.

Parameters: `project_id` (path, required)

Request body: `name?`: string | null; `description?`: string | null

Responses: `200` ProjectResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `status`: string; `is_locked`: boolean; `thumbnail_path`: string | null; `design_config?`: object | null; `editor_url`: string; `created_at`: date-time; `updated_at`: date-time

### `DELETE /api/v1/projects/{project_id}` — Chuyển vào thùng rác

Xóa mềm; giải phóng hạn mức dự án. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/projects/{project_id}/restore` — Khôi phục từ thùng rác

Trong 30 ngày; ngoài hạn trả `PROJ_RESTORE_EXPIRED`; còn phụ thuộc hạn mức gói hiện tại. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` ProjectResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `status`: string; `is_locked`: boolean; `thumbnail_path`: string | null; `design_config?`: object | null; `editor_url`: string; `created_at`: date-time; `updated_at`: date-time

### `DELETE /api/v1/projects/{project_id}/permanent` — Xóa vĩnh viễn

Xóa dự án trong thùng rác và mọi tệp. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `PUT /api/v1/projects/{project_id}/design` — Lưu thiết kế

Kiểm tra giới hạn layer (BR-52), khóa khi đang xuất (`PROJECT_EXPORTING`, BR-45) và guardrail nội dung (`CONTENT_BANNED`, `CONTENT_TRADEMARK_CONFIRM_REQUIRED`, `CONTENT_TEXT_TOO_LONG`). Mỗi lần lưu khác nội dung tạo một phiên bản. Header `X-Service-Token` bắt buộc. Service token (`X-Service-Token`).

Parameters: `project_id` (path, required), `x-service-token` (header, required)

Request body: `design_config`: object; `thumbnail_path?`: string | null; `base_revision`: integer

Responses: `200` MessageResponse, `404` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/projects/{project_id}/bake` — Bắt đầu bake/export 3D

Kiểm tra email đã xác thực, không trong ân hạn, hạn mức xuất; ghim phiên bản thiết kế đang gửi. Trả job (202). Đang có job trả `PROJ_BAKE_IN_PROGRESS`. Service token (`X-Service-Token`).

Parameters: `project_id` (path, required), `x-service-token` (header, required)

Request body: `design_config`: object

Responses: `202` BakeJobResponse, `404` ErrorResponse

Response fields: `job_id`: uuid; `status`: string; `priority`: string

### `GET /api/v1/projects/{project_id}/bake/{job_id}` — Trạng thái bake job

Trả trạng thái, thời gian, `can_retry`, `can_cancel`, `poll_after_seconds`. Bearer.

Parameters: `project_id` (path, required), `job_id` (path, required)

Responses: `200` BakeJobStatusResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `job_id`: uuid; `status`: string; `priority`: string; `error_message`: string | null; `queued_at`: date-time; `started_at`: date-time | null; `completed_at`: date-time | null; `can_retry?`: boolean; `can_cancel?`: boolean; `poll_after_seconds?`: integer | null

### `POST /api/v1/projects/{project_id}/bake/{job_id}/retry` — Thử lại bake job lỗi

Chỉ với job `failed`. Bearer.

Parameters: `project_id` (path, required), `job_id` (path, required)

Responses: `202` BakeJobResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `job_id`: uuid; `status`: string; `priority`: string

### `POST /api/v1/projects/{project_id}/bake/{job_id}/cancel` — Hủy bake job

Chỉ với job đang chờ (`queued`). Bearer.

Parameters: `project_id` (path, required), `job_id` (path, required)

Responses: `200` BakeJobResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `job_id`: uuid; `status`: string; `priority`: string

### `GET /api/v1/projects/{project_id}/exports` — File đã xuất của dự án

Các bản xuất (glb/obj/zip) của dự án. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` ExportListResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `items`: list[ExportResponse]

## Assets

Tệp của dự án (model 3D, texture) qua presigned URL.

### `POST /api/v1/projects/{project_id}/assets/upload-url` — Xin URL tải tệp lên

Trả presigned URL (15 phút) để tải model/texture; xác nhận bằng `/assets/confirm`. Bearer.

Parameters: `project_id` (path, required)

Request body: `asset_type`: string; `filename`: string; `content_type`: string

Responses: `200` AssetUploadURLResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `upload_url`: string; `asset_id`: uuid; `file_path`: string; `expires_in?`: integer

### `POST /api/v1/projects/{project_id}/assets/confirm` — Xác nhận tệp đã tải

Kiểm tra tệp tồn tại trong lưu trữ rồi ghi nhận vào dự án. Bearer.

Parameters: `project_id` (path, required)

Request body: `asset_id`: uuid; `file_size_bytes?`: integer | null

Responses: `200` AssetResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `project_id`: uuid; `asset_type`: string; `original_filename`: string | null; `file_path`: string; `file_size_bytes`: integer | null; `mime_type`: string | null; `status`: string; `created_at`: date-time

### `GET /api/v1/projects/{project_id}/assets` — Danh sách tệp của dự án

Model gốc, texture, thumbnail… Bearer.

Parameters: `project_id` (path, required)

Responses: `200` AssetListResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `items`: list[AssetResponse]

### `DELETE /api/v1/projects/{project_id}/assets/{asset_id}` — Xóa tệp

Xóa bản ghi và tệp lưu trữ. Bearer.

Parameters: `project_id` (path, required), `asset_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

## Editor

API của cầu nối KusStudio (desktop): dùng editor session token lấy từ vé mở editor.

### `GET /api/v1/editor/me` — Người dùng của editor session

Thông tin người dùng gắn với editor session. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Responses: `200` EditorUserResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `role`: string; `name`: string; `email`: string; `createdAt`: date-time; `updatedAt?`: date-time | null

### `GET /api/v1/editor/projects/{project_id}/context` — Ngữ cảnh mở editor

Người dùng, dự án, model gốc, thiết kế gần nhất và quyền. Dùng cho KusStudio desktop. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` EditorContextResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `project`: EditorProjectResponse; `modelAsset?`: EditorModelAssetResponse | null; `latestDesign?`: EditorDesignResponse | null; `permissions`: EditorPermissionsResponse

### `POST /api/v1/editor/projects/{project_id}/designs` — Lưu thiết kế từ editor

Cần scope `editor:write`. Kiểm tra model gốc sẵn sàng, `base_revision` khớp bản hiện tại (xung đột trả lỗi revision), khóa xuất (`PROJECT_EXPORTING`) và guardrail nội dung; mỗi lần lưu tạo revision và một phiên bản lịch sử. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `project_id` (path, required)

Request body: `designConfig`: EditorDesignConfig; `name?`: string | null; `baseRevision`: integer

Responses: `200` EditorDesignResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `userId`: uuid; `projectId`: uuid; `modelAssetId`: uuid; `name`: string; `status`: string; `revision`: integer; `designConfig`: object; `previewGlbUrl?`: string | null; `previewStatus?`: string; `previewErrorMessage?`: string | null; `createdAt`: date-time; `updatedAt`: date-time

### `GET /api/v1/editor/designs/{design_id}` — Lấy thiết kế

Thiết kế hiện tại của dự án kèm bake job và file xuất gần nhất. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `design_id` (path, required)

Responses: `200` EditorDesignResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `userId`: uuid; `projectId`: uuid; `modelAssetId`: uuid; `name`: string; `status`: string; `revision`: integer; `designConfig`: object; `previewGlbUrl?`: string | null; `previewStatus?`: string; `previewErrorMessage?`: string | null; `createdAt`: date-time; `updatedAt`: date-time

### `POST /api/v1/editor/designs/{design_id}/bake` — Bắt đầu bake từ editor

Cần scope `editor:write` và thiết kế đã lưu; dùng cùng quy tắc hạn mức như bake của portal (202). Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `design_id` (path, required)

Responses: `202` EditorJobResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `type?`: string; `status`: string; `progress`: integer; `errorMessage?`: string | null; `designId`: uuid; `projectId`: uuid; `createdAt`: date-time; `updatedAt`: date-time

### `GET /api/v1/editor/jobs/{job_id}` — Trạng thái bake job

Trạng thái xử lý của một bake job do editor tạo. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `job_id` (path, required)

Responses: `200` EditorJobResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `type?`: string; `status`: string; `progress`: integer; `errorMessage?`: string | null; `designId`: uuid; `projectId`: uuid; `createdAt`: date-time; `updatedAt`: date-time

### `POST /api/v1/editor/designs/{design_id}/export` — Lấy gói export

Trả các file đã xuất của thiết kế để editor tải về. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `design_id` (path, required)

Responses: `200` EditorExportPackageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `designId`: uuid; `status?`: string; `downloadUrl`: string; `zipUrl?`: string | null; `files`: list[string]; `createdAt`: date-time; `updatedAt?`: date-time | null

### `POST /api/v1/editor/assets/upload-url` — Xin URL tải tệp (editor)

Presigned URL để editor tải model/texture lên dự án. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Request body: `asset_type`: string; `filename`: string; `content_type`: string

Responses: `200` AssetUploadURLResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `upload_url`: string; `asset_id`: uuid; `file_path`: string; `expires_in?`: integer

### `POST /api/v1/editor/assets/confirm` — Xác nhận tệp đã tải (editor)

Xác nhận tệp đã có trong kho lưu trữ và ghi nhận vào dự án. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Request body: `asset_id`: uuid; `file_size_bytes?`: integer | null

Responses: `200` AssetResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `project_id`: uuid; `asset_type`: string; `original_filename`: string | null; `file_path`: string; `file_size_bytes`: integer | null; `mime_type`: string | null; `status`: string; `created_at`: date-time

### `GET /api/v1/editor/assets/{asset_id}/content` — Tải nội dung tệp

Stream nội dung một tệp của dự án; chỉ tệp thuộc phiên editor. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `asset_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `GET /api/v1/editor/exports/{export_id}/content` — Tải nội dung file xuất

Stream nội dung một file đã xuất; chỉ file thuộc phiên editor. Editor session token (Bearer, cấp bởi `/auth/editor/launch/exchange`).

Parameters: `export_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

## Mobile

Cầu nối ứng dụng di động (quét giày). Ngoài phạm vi web portal.

### `POST /api/v1/mobile/scans/bootstrap` — Khởi tạo phiên quét

Ứng dụng di động bắt đầu một phiên quét cho người dùng đã đăng nhập. Ngoài phạm vi web portal. Bearer.

Request body: `client_request_id`: uuid; `project_name?`: string

Responses: `201` MobileScanBootstrapResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `project_id`: uuid; `compute_api_url`: string; `compute_grant`: string; `expires_in`: integer; `web_project_url`: string

## Mobile Internal

Endpoint nội bộ cho dịch vụ tính toán quét; xác thực bằng token dịch vụ riêng.

### `POST /api/v1/internal/mobile/compute-grants/claim` — Nhận quyền tính toán quét

Dịch vụ tính toán nhận một compute grant. Token tính toán di động (dịch vụ nội bộ).

Parameters: `x-service-token` (header, required)

Request body: `compute_grant`: string

Responses: `200` MobileComputeGrantClaimResponse

Response fields: `user_id`: uuid; `project_id`: uuid; `project_name`: string; `completion_token`: string; `web_project_url`: string

### `POST /api/v1/internal/mobile/scans/output-upload` — Xin URL tải kết quả quét

Dịch vụ tính toán xin presigned URL để tải kết quả lên. Token tính toán di động (dịch vụ nội bộ).

Parameters: `x-service-token` (header, required)

Request body: `completion_token`: string

Responses: `200` MobileOutputUploadResponse

Response fields: `project_id`: uuid; `asset_id`: uuid; `file_path`: string; `upload_url`: string | null; `expires_in`: integer; `already_completed?`: boolean

### `POST /api/v1/internal/mobile/scans/output-confirm` — Xác nhận kết quả quét

Dịch vụ tính toán xác nhận kết quả đã tải lên. Token tính toán di động (dịch vụ nội bộ).

Parameters: `x-service-token` (header, required)

Request body: `completion_token`: string; `asset_id`: uuid; `file_size_bytes`: integer; `project_name?`: string | null

Responses: `200` MobileOutputConfirmResponse

Response fields: `project_id`: uuid; `model_asset_id`: uuid; `status`: string; `web_project_url`: string

## Studio

Lịch sử phiên bản, template, link chia sẻ cho nghệ nhân, gói tham khảo PDF.

### `GET /api/v1/projects/{project_id}/versions` — Lịch sử phiên bản

Các phiên bản thiết kế, mới nhất trước; bản đã xuất được ghim và không bị xóa khi dọn (BR-46). Bearer.

Parameters: `project_id` (path, required)

Responses: `200` list[DesignVersionItem], `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `GET /api/v1/projects/{project_id}/versions/{version_id}` — Chi tiết phiên bản

Kèm toàn bộ `design_config`. Bearer.

Parameters: `project_id` (path, required), `version_id` (path, required)

Responses: `200` DesignVersionDetail, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `version_no`: integer; `is_pinned`: boolean; `export_bake_job_id`: uuid | null; `thumbnail_path`: string | null; `created_at`: date-time; `design_config`: object

### `POST /api/v1/projects/{project_id}/versions/{version_id}/restore` — Khôi phục phiên bản

Ghi cấu hình cũ thành phiên bản mới nhất (lịch sử không bị sửa). Bị chặn khi dự án khóa hoặc đang xuất. Bearer.

Parameters: `project_id` (path, required), `version_id` (path, required)

Responses: `200` DesignVersionItem, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `version_no`: integer; `is_pinned`: boolean; `export_bake_job_id`: uuid | null; `thumbnail_path`: string | null; `created_at`: date-time

### `GET /api/v1/templates` — Thư viện template

Template đã duyệt, lọc theo `category`. Bearer.

Parameters: `category` (query)

Responses: `200` list[TemplateListItem], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/projects/{project_id}/apply-template/{template_id}` — Áp dụng template

Thay thiết kế bằng template, tôn trọng giới hạn layer, khóa và guardrail; tạo phiên bản mới. Bearer.

Parameters: `project_id` (path, required), `template_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `GET /api/v1/projects/{project_id}/artisan-links` — Danh sách link chia sẻ

Không trả token. Bearer.

Parameters: `project_id` (path, required)

Responses: `200` list[ArtisanLinkResponse], `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/projects/{project_id}/artisan-links` — Tạo link cho nghệ nhân

Chỉ gói trả phí đang `active` (không trong ân hạn), từ một bản xuất (mặc định bản mới nhất). Hiệu lực 30 ngày, tối đa 20 lượt tải (BR-101). Token chỉ hiển thị một lần trong phản hồi này. Bearer.

Parameters: `project_id` (path, required)

Request body: `export_id?`: uuid | null

Responses: `201` ArtisanLinkCreated, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `project_id`: uuid; `export_record_id`: uuid; `expires_at`: date-time; `max_downloads`: integer; `download_count`: integer; `revoked_at`: date-time | null; `is_active`: boolean; `token`: string; `url`: string

### `POST /api/v1/artisan-links/{link_id}/revoke` — Thu hồi link

Link ngừng hoạt động ngay. Bearer.

Parameters: `link_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `POST /api/v1/artisan-links/{link_id}/renew` — Gia hạn link

Đặt lại 30 ngày và số lượt tải. Bearer.

Parameters: `link_id` (path, required)

Responses: `200` ArtisanLinkResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `project_id`: uuid; `export_record_id`: uuid; `expires_at`: date-time; `max_downloads`: integer; `download_count`: integer; `revoked_at`: date-time | null; `is_active`: boolean

### `GET /api/v1/projects/{project_id}/reference-pack` — Tải gói tham khảo PDF

PDF gồm lưu ý, màu, chữ/font, danh sách layer, phiên bản và ngày; tùy chọn `token` để in mã QR tới trang xem (BR-71). Không có ảnh 4 góc. Bearer.

Parameters: `project_id` (path, required), `token` (query)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

## Public Artisan

Endpoint công khai (không đăng nhập) để nghệ nhân xem/tải bản xuất qua link chia sẻ.

### `GET /api/v1/public/artisan/{token}` — Xem thông tin link nghệ nhân

Tên dự án, định dạng, hạn và số lượt tải còn lại. Link sai/hết hạn/bị thu hồi/hết lượt đều trả 410 `ARTISAN_LINK_INVALID`. Giới hạn 30 lượt/phút/IP. Công khai.

Parameters: `token` (path, required)

Responses: `200` ArtisanPublicView, `404` ErrorResponse

Response fields: `project_name`: string; `format`: string; `expires_at`: date-time; `downloads_remaining`: integer

### `POST /api/v1/public/artisan/{token}/download` — Tải bản xuất qua link

Trừ một lượt và trả URL tải có chữ ký tối đa 15 phút. Lỗi giống endpoint xem. Công khai.

Parameters: `token` (path, required)

Responses: `200` ArtisanDownloadResponse, `404` ErrorResponse

Response fields: `download_url`: string; `expires_in_seconds`: integer

## Exports

Lịch sử file đã xuất và link tải.

### `GET /api/v1/exports` — Lịch sử xuất file

Lọc theo dự án/định dạng, phân trang con trỏ. Bearer.

Parameters: `project_id` (query), `format` (query), `limit` (query), `cursor` (query)

Responses: `200` ExportHistoryResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[ExportHistoryItem]; `next_cursor`: string | null; `has_next`: boolean

### `POST /api/v1/exports/{export_id}/download-url` — Lấy link tải file đã xuất

URL có chữ ký 1 giờ, tăng bộ đếm lượt tải. Bearer.

Parameters: `export_id` (path, required)

Responses: `200` ExportDownloadResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `download_url`: string; `expires_in?`: integer

## Subscription

Gói dịch vụ, thanh toán PayOS/MoMo, mã giảm giá, hóa đơn và biên nhận.

### `GET /api/v1/plans` — Bảng giá công khai

Gói đang bán, hạn mức và giá. Công khai.

Responses: `200` list[PlanResponse]

### `GET /api/v1/subscription` — Gói hiện tại

Trạng thái (active/grace/…), ngày hết hạn, ân hạn 3 ngày (BR-90). Bearer.

Responses: `200` SubscriptionResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `tier`: string; `status`: string; `started_at`: date-time; `expires_at`: date-time | null; `cancel_at_period_end`: boolean

### `GET /api/v1/subscription/invoices` — Lịch sử hóa đơn

Hóa đơn của người dùng kèm số biên nhận. Bearer.

Parameters: `limit` (query), `before` (query)

Responses: `200` list[InvoiceResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/subscription/checkout` — Tạo thanh toán

Tạo hóa đơn chờ và trả link PayOS hoặc MoMo. Nâng cấp giữa chu kỳ tính pro-rata (BR-24); có thể kèm `coupon_code` (không cộng dồn với pro-rata). Chỉ kích hoạt gói khi webhook hợp lệ. Bị chặn trong phiên đăng nhập thay. Bearer.

Request body: `tier`: string; `billing_cycle`: string; `gateway`: string; `coupon_code?`: string | null

Responses: `200` CheckoutResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `checkout_url`: string

### `POST /api/v1/subscription/cancel` — Hủy gia hạn

Không gọi cổng thanh toán (không có tự động trừ tiền, BR-25); gói hết hạn theo chu kỳ. Bearer.

Request body: `immediate?`: boolean

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse

### `GET /api/v1/subscription/invoices/{invoice_id}` — Chi tiết hóa đơn / trạng thái

Trang thanh toán thành công gọi lặp endpoint này đến khi hết `pending` (MSG29). Bearer.

Parameters: `invoice_id` (path, required)

Responses: `200` InvoiceResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `order_code`: integer; `plan_tier`: string; `billing_cycle`: string; `listed_price_vnd`: integer; `discount_vnd`: integer; `amount_vnd`: integer; `payment_method`: string; `status`: string; `receipt_number?`: string | null; `paid_at`: date-time | null; `created_at`: date-time

### `GET /api/v1/subscription/invoices/{invoice_id}/receipt` — Tải biên nhận PDF

Trả URL có chữ ký 15 phút tới biên nhận KUS-xxx bất biến; chưa phát hành trả 409. Bearer.

Parameters: `invoice_id` (path, required)

Responses: `200` ReceiptResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `receipt_number`: string | null; `download_url`: string; `expires_in?`: integer

### `POST /api/v1/subscription/coupon/preview` — Xem trước mã giảm giá

Tính giá sau giảm cho một gói mà không tạo hóa đơn; mỗi tài khoản dùng một mã một lần. Bearer.

Request body: `tier`: string; `billing_cycle`: string; `coupon_code`: string

Responses: `200` CouponPreviewResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `listed_price_vnd`: integer; `discount_vnd`: integer; `amount_vnd`: integer

## Feedback

Gửi và xem phản hồi của khách hàng.

### `GET /api/v1/feedback/eligibility` — Có được gửi phản hồi không

Mỗi người 1 phiếu/14 ngày (BR-109); trả `next_allowed_at`. Bearer.

Responses: `200` FeedbackEligibility, `401` ErrorResponse, `403` ErrorResponse

Response fields: `can_submit`: boolean; `next_allowed_at`: date-time | null

### `POST /api/v1/feedback` — Gửi phản hồi

Điểm 1–5, nội dung và nhóm 4P. Gửi sớm trả 429 `FEEDBACK_TOO_SOON` kèm `next_allowed_at`. Bearer.

Request body: `rating`: integer; `message`: string; `marketing_group?`: string

Responses: `201` FeedbackResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `rating`: integer; `marketing_group`: string; `message`: string; `status`: string; `changed_what`: string | null; `created_at`: date-time

### `GET /api/v1/feedback/mine` — Phản hồi của tôi

Kèm trạng thái xử lý và 'đã thay đổi gì'. Bearer.

Responses: `200` list[FeedbackResponse], `401` ErrorResponse, `403` ErrorResponse

## Webhooks

Callback từ cổng thanh toán (xác thực bằng chữ ký, không dùng Bearer).

### `POST /api/v1/webhooks/payos` — Webhook PayOS

Xác thực chữ ký HMAC-SHA256, khớp số tiền, idempotent. Không dùng Bearer. Công khai.

Responses: `200` object

### `POST /api/v1/webhooks/momo` — Webhook (IPN) MoMo

Xác thực chữ ký HMAC-SHA256, khớp số tiền, idempotent. Không dùng Bearer. Công khai.

Responses: `200` object

## Admin Auth

Đăng nhập/đăng xuất khu vực quản trị.

### `POST /api/v1/admin/auth/login` — Đăng nhập quản trị

Đăng nhập cho role admin/staff, giới hạn tần suất theo IP. Công khai.

Request body: `email`: email; `password`: string

Responses: `200` AdminLoginResponse

Response fields: `access_token`: string; `token_type?`: string; `role`: string

### `POST /api/v1/admin/auth/logout` — Đăng xuất quản trị

Thu hồi phiên quản trị hiện tại. Admin (Bearer).

Request body: 

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse

## Admin Users

Quản lý người dùng, nhân viên, cấp gói tặng, đăng nhập thay, đặt lại mật khẩu.

### `GET /api/v1/admin/users` — Danh sách người dùng

Tìm kiếm, lọc theo trạng thái/role, phân trang con trỏ. Admin (Bearer).

Parameters: `q` (query), `status` (query), `role` (query), `include_deleted` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminUserListItem_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminUserListItem]; `next_cursor`: string | null

### `GET /api/v1/admin/users/{user_id}` — Chi tiết người dùng

Hồ sơ, gói, hạn mức và thống kê sử dụng. Admin (Bearer).

Parameters: `user_id` (path, required)

Responses: `200` AdminUserDetailResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `email`: string; `username`: string; `account_code`: string; `role`: string; `status`: string; `is_verified`: boolean; `is_internal`: boolean; `deleted_at`: date-time | null; `created_at`: date-time; `first_name`: string | null; `last_name`: string | null; `subscription_tier`: string | null; `subscription_status`: string | null; `subscription_expires_at`: date-time | null; `projects_count_this_month`: integer; `exports_count_this_month`: integer; `total_projects`: integer

### `POST /api/v1/admin/users/{user_id}/ban` — Khóa tài khoản

Đặt trạng thái `suspended` (chặn đăng nhập ngay). Không áp dụng cho admin/staff hoặc chính mình. Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Request body: `reason?`: string | null

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/users/{user_id}/unban` — Mở khóa tài khoản

Đặt lại trạng thái `active`. Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/users/{user_id}/internal` — Gắn/bỏ cờ tài khoản nội bộ

Tài khoản nội bộ bị loại khỏi mọi chỉ số kinh doanh (BR-83). Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Request body: `is_internal`: boolean

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/staff` — Tạo tài khoản staff

Tạo tài khoản role `staff` (chỉ xem, không ghi). Admin ghi (Bearer, staff bị 403).

Request body: `email`: email; `username`: string; `password`: string; `first_name`: string; `last_name`: string

Responses: `201` StaffCreateResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `email`: string; `username`: string; `account_code`: string; `role`: string

### `POST /api/v1/admin/users/{user_id}/grant-plan` — Cấp gói tặng (COMP)

Cấp gói miễn phí có thời hạn, không tạo giao dịch và không tính vào doanh thu (BR-103). Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Request body: `tier`: string; `billing_cycle?`: string; `days?`: integer; `reason`: string

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/users/{user_id}/impersonate` — Đăng nhập thay người dùng

Yêu cầu admin đã bật 2FA và nhập lý do/mã ticket (BR-80). Trả access token 30 phút, không có refresh token; phiên bị chặn thanh toán, đổi mật khẩu, xóa tài khoản, 2FA. Ghi audit và gửi email cho khách khi kết thúc. Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Request body: `reason`: string

Responses: `200` ImpersonateResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `access_token`: string; `expires_at`: date-time; `target_user_id`: uuid; `banner`: string

### `POST /api/v1/admin/users/{user_id}/reset-password` — Gửi mã đặt lại mật khẩu cho khách

Admin không thấy/đặt mật khẩu; khách nhận mã khôi phục qua email. Tài khoản chỉ dùng Google trả `ADMIN_RESET_NOT_ALLOWED`. Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

## Admin Plans

Cấu hình gói dịch vụ và hạn mức.

### `GET /api/v1/admin/plans` — Danh sách gói

Kể cả gói đã ẩn, kèm toàn bộ hạn mức. Admin (Bearer).

Responses: `200` list[AdminPlanResponse], `401` ErrorResponse, `403` ErrorResponse

### `PATCH /api/v1/admin/plans/{plan_id}` — Cập nhật gói

Sửa giá, hạn mức, định dạng xuất, ưu tiên bake, số phiên bản tối đa mỗi dự án. Admin ghi (Bearer, staff bị 403).

Parameters: `plan_id` (path, required)

Request body: `price_vnd?`: integer | null; `max_projects?`: integer | null; `max_exports_per_month?`: integer | null; `allowed_export_formats?`: list[string] | null; `bake_priority?`: string | null; `is_active?`: boolean | null; `max_ai_credits_per_cycle?`: integer | null; `max_scans_per_cycle?`: integer | null; `max_layers_per_zone?`: integer | null; `max_layers_per_project?`: integer | null; `allow_draw_artwork?`: boolean | null; `max_versions_per_project?`: integer | null

Responses: `200` AdminPlanResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `tier`: string; `billing_cycle`: string | null; `price_vnd`: integer; `max_projects`: integer | null; `max_exports_per_month`: integer | null; `allowed_export_formats`: list[string]; `bake_priority`: string; `is_active`: boolean; `max_ai_credits_per_cycle`: integer | null; `max_scans_per_cycle`: integer | null; `max_layers_per_zone`: integer; `max_layers_per_project`: integer; `allow_draw_artwork`: boolean; `max_versions_per_project`: integer

## Admin Billing

Giao dịch, hoàn tiền, giao dịch thủ công, khóa kỳ báo cáo, mã giảm giá.

### `GET /api/v1/admin/billing/subscriptions` — Danh sách đăng ký

Lọc theo trạng thái/gói, phân trang con trỏ. Admin (Bearer).

Parameters: `tier` (query), `status` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminSubscriptionResponse_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminSubscriptionResponse]; `next_cursor`: string | null

### `GET /api/v1/admin/billing/invoices` — Danh sách giao dịch

Lọc theo trạng thái, hình thức thanh toán, giao dịch thủ công, cờ nội bộ, khoảng ngày; phân trang con trỏ. Admin (Bearer).

Parameters: `status` (query), `user_id` (query), `payment_method` (query), `is_manual` (query), `exclude_internal` (query), `date_from` (query), `date_to` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminInvoiceResponse_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminInvoiceResponse]; `next_cursor`: string | null

### `POST /api/v1/admin/billing/subscriptions/{user_id}/force-downgrade` — Ép hạ về Free

Hạ gói của người dùng về Free ngay (khóa dự án vượt hạn mức, BR-27). Ghi audit. Admin ghi (Bearer, staff bị 403).

Parameters: `user_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/billing/invoices/{invoice_id}/refund` — Hoàn tiền một giao dịch

Tạo bút toán hoàn tiền (không gọi cổng thanh toán). Tự động chỉ khi ≤7 ngày và chưa xuất file; ngoài chính sách cần `override=true` (BR-97). Hoàn toàn bộ gói hiện tại sẽ hạ về Free. Bị chặn nếu ngày thanh toán nằm trong kỳ đã khóa (`PERIOD_LOCKED`). Admin ghi (Bearer, staff bị 403).

Parameters: `invoice_id` (path, required)

Request body: `amount_vnd`: integer; `reason`: string; `override?`: boolean

Responses: `200` RefundResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `status`: string; `refund_id`: uuid

### `POST /api/v1/admin/billing/manual-transactions/proof-upload` — Xin URL tải ảnh chứng từ

Trả presigned URL để tải ảnh chứng từ (chuyển khoản/tiền mặt) trước khi tạo giao dịch thủ công. Admin ghi (Bearer, staff bị 403).

Request body: `filename`: string; `content_type`: string

Responses: `200` ProofUploadResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `upload_url`: string; `file_path`: string; `expires_in?`: integer

### `POST /api/v1/admin/billing/manual-transactions` — Tạo giao dịch thủ công

Ghi nhận thanh toán tiền mặt/chuyển khoản trực tiếp ở trạng thái chờ duyệt; cần người thứ hai duyệt (maker/checker, BR-95). Admin ghi (Bearer, staff bị 403).

Request body: `user_id`: uuid; `tier`: string; `billing_cycle?`: string; `amount_vnd`: integer; `paid_on`: date; `collected_by`: string; `proof_path`: string; `reason`: string

Responses: `201` AdminInvoiceResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `order_code`: integer; `plan_tier`: string; `billing_cycle`: string; `listed_price_vnd`: integer; `discount_vnd`: integer; `amount_vnd`: integer; `payment_method`: string; `status`: string; `receipt_number?`: string | null; `paid_at`: date-time | null; `created_at`: date-time; `user_id`: uuid; `user_email`: string | null; `payment_reference`: string | null; `coupon_code?`: string | null; `is_manual?`: boolean; `collected_by?`: string | null; `created_by?`: uuid | null; `approved_by?`: uuid | null

### `POST /api/v1/admin/billing/invoices/{invoice_id}/approve` — Duyệt giao dịch thủ công

Người duyệt phải khác người tạo. Duyệt xong kích hoạt gói và sinh biên nhận KUS-xxx. Admin ghi (Bearer, staff bị 403).

Parameters: `invoice_id` (path, required)

Responses: `200` AdminInvoiceResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `order_code`: integer; `plan_tier`: string; `billing_cycle`: string; `listed_price_vnd`: integer; `discount_vnd`: integer; `amount_vnd`: integer; `payment_method`: string; `status`: string; `receipt_number?`: string | null; `paid_at`: date-time | null; `created_at`: date-time; `user_id`: uuid; `user_email`: string | null; `payment_reference`: string | null; `coupon_code?`: string | null; `is_manual?`: boolean; `collected_by?`: string | null; `created_by?`: uuid | null; `approved_by?`: uuid | null

### `POST /api/v1/admin/billing/invoices/{invoice_id}/reject` — Từ chối giao dịch thủ công

Từ chối kèm lý do; giao dịch chuyển sang thất bại. Admin ghi (Bearer, staff bị 403).

Parameters: `invoice_id` (path, required)

Request body: `reason`: string

Responses: `200` AdminInvoiceResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `order_code`: integer; `plan_tier`: string; `billing_cycle`: string; `listed_price_vnd`: integer; `discount_vnd`: integer; `amount_vnd`: integer; `payment_method`: string; `status`: string; `receipt_number?`: string | null; `paid_at`: date-time | null; `created_at`: date-time; `user_id`: uuid; `user_email`: string | null; `payment_reference`: string | null; `coupon_code?`: string | null; `is_manual?`: boolean; `collected_by?`: string | null; `created_by?`: uuid | null; `approved_by?`: uuid | null

### `GET /api/v1/admin/billing/periods` — Danh sách kỳ báo cáo

Các kỳ mở/khóa dùng để khóa sổ (BR-98). Admin (Bearer).

Responses: `200` list[ReportingPeriodResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/admin/billing/periods` — Tạo kỳ báo cáo

Tạo kỳ theo khoảng ngày (GMT+7), trạng thái mở. Admin ghi (Bearer, staff bị 403).

Request body: `name`: string; `start_date`: date; `end_date`: date

Responses: `201` ReportingPeriodResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `name`: string; `start_date`: date; `end_date`: date; `status`: string; `locked_by`: uuid | null; `locked_at`: date-time | null

### `POST /api/v1/admin/billing/periods/{period_id}/lock` — Khóa kỳ báo cáo

Sau khi khóa, giao dịch thủ công và hoàn tiền trong kỳ bị từ chối. Admin ghi (Bearer, staff bị 403).

Parameters: `period_id` (path, required)

Responses: `200` ReportingPeriodResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `start_date`: date; `end_date`: date; `status`: string; `locked_by`: uuid | null; `locked_at`: date-time | null

### `GET /api/v1/admin/billing/coupons` — Danh sách mã giảm giá

Toàn bộ mã kèm số lần đã dùng. Admin (Bearer).

Responses: `200` list[CouponResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/admin/billing/coupons` — Tạo mã giảm giá

Loại `percent`, `fixed` hoặc `fixed_price`; giới hạn gói, thời hạn, số lượt; mỗi tài khoản dùng một lần. Admin ghi (Bearer, staff bị 403).

Request body: `code`: string; `discount_type`: string; `value`: integer; `plan_tiers?`: list[string] | null; `max_uses?`: integer | null; `valid_from?`: date-time | null; `valid_until?`: date-time | null

Responses: `201` CouponResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `code`: string; `discount_type`: string; `value`: integer; `plan_tiers`: list[string] | null; `max_uses`: integer | null; `used_count`: integer; `valid_from`: date-time | null; `valid_until`: date-time | null; `is_active`: boolean

### `PATCH /api/v1/admin/billing/coupons/{coupon_id}` — Cập nhật mã giảm giá

Sửa hạn dùng, giới hạn, bật/tắt mã. Admin ghi (Bearer, staff bị 403).

Parameters: `coupon_id` (path, required)

Request body: `value?`: integer | null; `plan_tiers?`: list[string] | null; `max_uses?`: integer | null; `valid_from?`: date-time | null; `valid_until?`: date-time | null; `is_active?`: boolean | null

Responses: `200` CouponResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `code`: string; `discount_type`: string; `value`: integer; `plan_tiers`: list[string] | null; `max_uses`: integer | null; `used_count`: integer; `valid_from`: date-time | null; `valid_until`: date-time | null; `is_active`: boolean

## Admin Dashboard

Số liệu tổng quan cho trang chủ quản trị.

### `GET /api/v1/admin/dashboard/stats` — Số liệu tổng quan

Người dùng, MRR, lượt xuất, dự án… cho trang chủ quản trị. Admin (Bearer).

Responses: `200` AdminStatsResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `total_users`: integer; `mrr_vnd`: integer; `total_exports`: integer

### `GET /api/v1/admin/dashboard/revenue` — Doanh thu theo tháng

Chuỗi doanh thu `months` tháng gần nhất (mặc định 12). Admin (Bearer).

Parameters: `months` (query)

Responses: `200` list[MonthlyPoint], `401` ErrorResponse, `403` ErrorResponse

### `GET /api/v1/admin/dashboard/user-growth` — Tăng trưởng người dùng

Số đăng ký mới theo tháng (mặc định 6 tháng). Admin (Bearer).

Parameters: `months` (query)

Responses: `200` list[MonthlyPoint], `401` ErrorResponse, `403` ErrorResponse

### `GET /api/v1/admin/dashboard/recent-users` — Người dùng mới nhất

Danh sách đăng ký gần đây. Admin (Bearer).

Parameters: `limit` (query)

Responses: `200` list[AdminRecentUserItem], `401` ErrorResponse, `403` ErrorResponse

## Admin Analytics

Chỉ số kinh doanh (MRR, ARR, churn, NRR/GRR…) và báo cáo CSV/XLSX/PDF.

### `GET /api/v1/admin/analytics` — Chỉ số kinh doanh

MRR, ARR, ARPU, doanh thu ghi nhận (so kỳ trước), churn, NRR/GRR, Free→trả phí, tỷ lệ khách quay lại (BR-105), thanh toán lỗi, doanh thu theo gói, biến động, top khách hàng. Loại tài khoản nội bộ và gói COMP. Mặc định 30 ngày gần nhất. Admin (Bearer).

Parameters: `date_from` (query), `date_to` (query)

Responses: `200` AnalyticsResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `date_from`: date; `date_to`: date; `mrr_vnd`: integer; `arr_vnd`: integer; `arpu_vnd`: integer; `paying_customers`: integer; `revenue_vnd`: PeriodValue; `refunds_vnd`: PeriodValue; `new_paying_customers`: PeriodValue; `churn`: ChurnMetric; `retention`: RetentionMetric; `free_to_paid`: RateMetric; `repeat`: RepeatMetric; `failed_payments`: FailedPayments; `revenue_by_plan`: list[PlanRevenue]; `revenue_series`: list[RevenuePoint]; `mrr_movement`: MrrMovement; `top_customers`: list[TopCustomer]

### `GET /api/v1/admin/reports/{report_type}` — Tải báo cáo

`report_type`: `revenue`, `users`, `transactions` (Sổ giao dịch EXE201, BR-106), `channel-funnel` (phễu theo kênh và tuần, BR-107). `format`: csv, xlsx hoặc pdf. Admin (Bearer).

Parameters: `report_type` (path, required), `format` (query), `date_from` (query), `date_to` (query)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

## Admin Studio

Quy tắc nội dung (guardrail) và thư viện template.

### `GET /api/v1/admin/guardrail-rules` — Danh sách quy tắc nội dung

Từ cấm (`banned`) và thương hiệu (`trademark`). Admin (Bearer).

Responses: `200` list[GuardrailRuleResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/admin/guardrail-rules` — Thêm quy tắc nội dung

Từ khóa được chuẩn hóa (không dấu, chữ thường); trùng trả `GUARDRAIL_RULE_EXISTS`. Ghi audit. Admin ghi (Bearer, staff bị 403).

Request body: `kind`: string; `term`: string

Responses: `201` GuardrailRuleResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `kind`: string; `term`: string; `is_active`: boolean

### `PATCH /api/v1/admin/guardrail-rules/{rule_id}` — Bật/tắt quy tắc

Đổi `is_active`. Ghi audit. Admin ghi (Bearer, staff bị 403).

Parameters: `rule_id` (path, required)

Request body: `is_active`: boolean

Responses: `200` GuardrailRuleResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `kind`: string; `term`: string; `is_active`: boolean

### `DELETE /api/v1/admin/guardrail-rules/{rule_id}` — Xóa quy tắc

Xóa quy tắc khỏi danh sách áp dụng. Ghi audit. Admin ghi (Bearer, staff bị 403).

Parameters: `rule_id` (path, required)

Responses: `200` MessageResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `message`: string

### `GET /api/v1/admin/templates` — Danh sách template (admin)

Lọc theo trạng thái pending/approved/rejected. Admin (Bearer).

Parameters: `status` (query)

Responses: `200` list[AdminTemplateResponse], `401` ErrorResponse, `403` ErrorResponse

### `POST /api/v1/admin/templates` — Tạo template

Nội dung phải qua guardrail (từ cấm/thương hiệu bị từ chối). Tạo ở trạng thái chờ duyệt. Admin ghi (Bearer, staff bị 403).

Request body: `name`: string; `description?`: string | null; `category?`: string | null; `design_config`: object; `thumbnail_path?`: string | null

Responses: `201` AdminTemplateResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `category`: string | null; `thumbnail_path`: string | null; `layer_count`: integer; `use_count`: integer; `status`: string; `created_at`: date-time

### `POST /api/v1/admin/templates/{template_id}/approve` — Duyệt template

Hiển thị công khai trong thư viện. Admin ghi (Bearer, staff bị 403).

Parameters: `template_id` (path, required)

Responses: `200` AdminTemplateResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `category`: string | null; `thumbnail_path`: string | null; `layer_count`: integer; `use_count`: integer; `status`: string; `created_at`: date-time

### `POST /api/v1/admin/templates/{template_id}/reject` — Từ chối template

Ẩn khỏi thư viện. Admin ghi (Bearer, staff bị 403).

Parameters: `template_id` (path, required)

Responses: `200` AdminTemplateResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `description`: string | null; `category`: string | null; `thumbnail_path`: string | null; `layer_count`: integer; `use_count`: integer; `status`: string; `created_at`: date-time

## Admin Feedback

Xử lý phản hồi khách hàng và xuất Excel.

### `GET /api/v1/admin/feedback` — Danh sách phản hồi

Lọc theo trạng thái, nhóm 4P, số sao; mặc định loại tài khoản nội bộ (`include_internal`). Admin (Bearer).

Parameters: `status` (query), `marketing_group` (query), `rating` (query), `include_internal` (query)

Responses: `200` list[AdminFeedbackResponse], `401` ErrorResponse, `403` ErrorResponse

### `GET /api/v1/admin/feedback/summary` — Thống kê phản hồi

Số lượng, điểm trung bình, theo trạng thái và nhóm 4P (loại nội bộ). Admin (Bearer).

Responses: `200` FeedbackSummary, `401` ErrorResponse, `403` ErrorResponse

Response fields: `count`: integer; `average_rating`: number; `by_status`: object; `by_group`: object

### `GET /api/v1/admin/feedback/export` — Xuất phản hồi ra Excel

Tệp .xlsx; mặc định loại tài khoản nội bộ. Admin (Bearer).

Parameters: `include_internal` (query)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse

### `PATCH /api/v1/admin/feedback/{feedback_id}` — Xử lý phản hồi

Đổi trạng thái NEW→REVIEWED→PLANNED→DONE/WONT_DO và ghi 'đã thay đổi gì' hiển thị cho khách. Ghi audit. Admin ghi (Bearer, staff bị 403).

Parameters: `feedback_id` (path, required)

Request body: `status?`: string | null; `changed_what?`: string | null

Responses: `200` AdminFeedbackResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `rating`: integer; `marketing_group`: string; `message`: string; `status`: string; `changed_what`: string | null; `created_at`: date-time; `user_id`: uuid; `user_email`: string | null; `is_internal`: boolean; `reviewed_at`: date-time | null

## Admin Ops

Vận hành: dự án, bake job, export, sức khỏe hệ thống, nhật ký thao tác.

### `GET /api/v1/admin/projects` — Danh sách dự án

Mọi dự án kể cả đã xóa mềm; tìm kiếm, phân trang. Admin (Bearer).

Parameters: `user_id` (query), `status` (query), `q` (query), `include_deleted` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminProjectListItem_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminProjectListItem]; `next_cursor`: string | null

### `GET /api/v1/admin/projects/{project_id}` — Chi tiết dự án

Thông tin dự án, tệp và bake job. Admin (Bearer).

Parameters: `project_id` (path, required)

Responses: `200` AdminProjectDetailResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `name`: string; `status`: string; `user_id`: uuid; `owner_email`: string | null; `deleted_at`: date-time | null; `created_at`: date-time; `updated_at`: date-time; `description`: string | null; `asset_count`: integer; `latest_bake_status`: string | null

### `DELETE /api/v1/admin/projects/{project_id}` — Xóa dự án

Chuyển vào thùng rác (xóa mềm), dọn hẳn sau thời hạn lưu 30 ngày. Ghi audit. Admin ghi (Bearer, staff bị 403).

Parameters: `project_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `GET /api/v1/admin/bake-jobs` — Danh sách bake job

Lọc theo trạng thái, ưu tiên, dự án. Admin (Bearer).

Parameters: `status` (query), `priority` (query), `project_id` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminBakeJobResponse_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminBakeJobResponse]; `next_cursor`: string | null

### `GET /api/v1/admin/bake-jobs/{job_id}` — Chi tiết bake job

Trạng thái, lỗi, thời gian xử lý. Admin (Bearer).

Parameters: `job_id` (path, required)

Responses: `200` AdminBakeJobDetailResponse, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

Response fields: `id`: uuid; `project_id`: uuid; `project_name`: string | null; `status`: string; `priority`: string; `error_message`: string | null; `worker_id`: string | null; `queued_at`: date-time; `started_at`: date-time | null; `completed_at`: date-time | null; `design_config_snapshot`: object

### `POST /api/v1/admin/bake-jobs/{job_id}/requeue` — Xếp lại bake job lỗi

Chỉ với job `failed`. Admin ghi (Bearer, staff bị 403).

Parameters: `job_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `POST /api/v1/admin/bake-jobs/{job_id}/cancel` — Hủy bake job đang chờ

Chỉ với job `queued`. Admin ghi (Bearer, staff bị 403).

Parameters: `job_id` (path, required)

Responses: `200` object, `401` ErrorResponse, `403` ErrorResponse, `404` ErrorResponse

### `GET /api/v1/admin/exports` — Danh sách file xuất

Lọc theo người dùng, dự án, định dạng. Admin (Bearer).

Parameters: `user_id` (query), `project_id` (query), `format` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AdminExportRecordResponse_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AdminExportRecordResponse]; `next_cursor`: string | null

### `GET /api/v1/admin/system/health` — Sức khỏe hệ thống

Kiểm tra DB, Redis và độ sâu các hàng đợi (high/normal/low). Admin (Bearer).

Responses: `200` SystemHealthResponse, `401` ErrorResponse, `403` ErrorResponse

Response fields: `status`: string; `checks`: object; `queue_depths`: object; `bake_jobs_by_status`: object

### `GET /api/v1/admin/audit-logs` — Nhật ký thao tác quản trị

Lọc theo người thực hiện, hành động, đối tượng; phân trang con trỏ. Admin (Bearer).

Parameters: `actor_id` (query), `action` (query), `target_type` (query), `target_id` (query), `q` (query), `before` (query), `limit` (query), `cursor` (query)

Responses: `200` CursorPage_AuditLogResponse_, `401` ErrorResponse, `403` ErrorResponse

Response fields: `items`: list[AuditLogResponse]; `next_cursor`: string | null

## Health

Kiểm tra sống/sẵn sàng của dịch vụ.

### `GET /health` — Kiểm tra sống

Luôn trả `ok` nếu tiến trình chạy. Công khai.

Responses: `200` object

### `GET /health/ready` — Kiểm tra sẵn sàng

Kiểm tra kết nối DB, Redis và lưu trữ; `status` là `degraded` nếu một mục lỗi. Công khai.

Responses: `200` object

## Monitoring

Metrics dạng Prometheus.

### `GET /metrics` — Metrics Prometheus

Số request, độ trễ theo route. Công khai.

Responses: `200` object
